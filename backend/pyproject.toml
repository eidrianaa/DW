[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "adri-financial-dw"
version = "1.0.0"
description = "Acme Ltd Financial Data Warehouse - DDD + CQRS"
requires-python = ">=3.12"
